import React from 'react';
import Header from '../../components/Header';

const Contacts = () => {
    return (
        <div>
            <Header/>
            Contacts Page
        </div>
    );
};

export default Contacts;